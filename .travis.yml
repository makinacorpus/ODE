language: python

python:
  - 2.7

install:
 - pip install coverage
 - pip install flake8 --use-mirrors
 - python setup.py develop

before_script:
 - make flake8

script:
 - make test
 - make coverage

after_success:
 - pip install coveralls
 - coveralls
